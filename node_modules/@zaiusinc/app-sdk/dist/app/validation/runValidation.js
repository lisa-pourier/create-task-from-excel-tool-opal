"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const chalk_1 = __importDefault(require("chalk"));
const path = __importStar(require("path"));
const Runtime_1 = require("../Runtime");
const validateApp_1 = require("./validateApp");
Runtime_1.Runtime.initialize(path.resolve(process.cwd(), 'dist'), true)
    .then(async (runtime) => {
    try {
        const errors = await (0, validateApp_1.validateApp)(runtime);
        if (errors.length > 0) {
            console.error(chalk_1.default.red(`Validation failed:\n${errors.map((e) => ` * ${e}`).join('\n')}`));
            process.exit(1);
        }
        else {
            console.log(chalk_1.default.green('Looks good to me'));
        }
    }
    catch (e) {
        console.error(chalk_1.default.red(`Validation process failed: ${e.message}`));
        process.exit(1);
    }
})
    .catch((e) => {
    console.error(chalk_1.default.red(`Validation process failed: ${e.message}`));
    process.exit(1);
});
//# sourceMappingURL=runValidation.js.map