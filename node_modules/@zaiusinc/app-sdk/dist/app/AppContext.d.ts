import { AppManifest } from './types';
export interface AppContext {
    manifest: AppManifest;
    trackerId: string;
    installId: number;
    account?: {
        organizationId: string;
        instanceId: string;
    };
}
export declare function setContext(context: AppContext): void;
/**
 * Get the app context for the current request/job
 */
export declare function getAppContext(): AppContext;
/**
 * Check if the current context is for a global function request
 */
export declare function isGlobalContext(): boolean;
//# sourceMappingURL=AppContext.d.ts.map