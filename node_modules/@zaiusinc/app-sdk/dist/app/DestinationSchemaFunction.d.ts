import { DestinationSchema } from './types';
export interface DestinationSchemaFunctionConfig {
    destinationKey: string;
}
export declare abstract class DestinationSchemaFunction {
    protected config: DestinationSchemaFunctionConfig;
    constructor(config: DestinationSchemaFunctionConfig);
    abstract getDestinationsSchema(): Promise<DestinationSchema>;
}
//# sourceMappingURL=DestinationSchemaFunction.d.ts.map