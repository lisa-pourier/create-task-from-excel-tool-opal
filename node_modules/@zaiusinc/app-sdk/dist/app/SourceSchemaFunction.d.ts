import { SourceSchema } from './types';
export interface SourceSchemaFunctionConfig {
    sourceKey: string;
}
export declare abstract class SourceSchemaFunction {
    protected config: SourceSchemaFunctionConfig;
    constructor(config: SourceSchemaFunctionConfig);
    abstract getSourcesSchema(): Promise<SourceSchema>;
}
//# sourceMappingURL=SourceSchemaFunction.d.ts.map