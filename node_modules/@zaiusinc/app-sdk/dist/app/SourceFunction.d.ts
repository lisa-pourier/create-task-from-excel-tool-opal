import { Source } from '../sources/Source';
import { Request } from './lib/Request';
import { Response } from './lib/Response';
export interface SourceConfiguration {
    dataSyncId: string;
    sourceKey: string;
    schema: string | {
        entry_point: string;
    };
    webhookUrl?: string;
}
export declare abstract class SourceFunction {
    protected config: SourceConfiguration;
    protected request: Request;
    protected source: Source;
    constructor(config: SourceConfiguration, request: Request, source: Source);
    abstract perform(): Promise<Response>;
}
//# sourceMappingURL=SourceFunction.d.ts.map