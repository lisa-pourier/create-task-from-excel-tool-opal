"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
require("jest");
const store_1 = require("../../../store");
const forms_1 = require("../forms");
describe('Forms', () => {
    it('sets and retrieves openSection', async () => {
        expect(await store_1.storage.settings.get('$formState')).toEqual({});
        expect(await forms_1.Form.getDefaultSection()).toBeUndefined();
        await forms_1.Form.setDefaultSection('foo');
        expect(await store_1.storage.settings.get('$formState')).toEqual({ defaultSection: 'foo' });
        expect(await forms_1.Form.getDefaultSection()).toEqual('foo');
        await forms_1.Form.clearDefaultSection();
        expect(await store_1.storage.settings.get('$formState')).toEqual({});
        expect(await forms_1.Form.getDefaultSection()).toBeUndefined();
    });
});
//# sourceMappingURL=forms.test.js.map