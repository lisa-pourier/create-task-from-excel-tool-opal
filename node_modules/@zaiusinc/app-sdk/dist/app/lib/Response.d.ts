import { Headers } from './Headers';
export declare class Response {
    status: number;
    headers: Headers;
    private bodyData?;
    constructor(status?: number, bodyJSON?: any, headers?: Headers);
    set body(body: string | null);
    get bodyAsU8Array(): Uint8Array | undefined;
    set bodyAsU8Array(body: Uint8Array | undefined);
    set bodyJSON(body: any);
}
//# sourceMappingURL=Response.d.ts.map