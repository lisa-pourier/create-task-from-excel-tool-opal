/**
 * @hidden
 */
export interface LiquidExtensionResponse {
    output?: string;
    error?: string;
}
/**
 * Result of {@link LiquidExtension.perform}. Clients should use the static {@link success} and {@link error} helper
 * methods (as opposed to constructing manually).
 */
export declare class LiquidExtensionResult {
    private success;
    private output?;
    private message?;
    /**
     * Builds a success response.
     * @param output the resulting output (must be JSON-serializable)
     */
    static success(output: any): LiquidExtensionResult;
    /**
     * Builds an error response.
     * @param message
     */
    static error(message: string): LiquidExtensionResult;
    /**
     * @hidden
     * Use the static helper methods instead.
     */
    constructor(success: boolean, output?: any | undefined, message?: string | undefined);
    /**
     * @hidden
     * Used internally to get the complete response
     */
    getResponse(): LiquidExtensionResponse;
}
//# sourceMappingURL=LiquidExtensionResult.d.ts.map