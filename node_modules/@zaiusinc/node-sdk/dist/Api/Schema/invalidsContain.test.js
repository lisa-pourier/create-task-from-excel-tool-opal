"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
require("jest");
const invalidsContain_1 = require("./invalidsContain");
describe('invalidsContain', () => {
    const invalids = [
        {
            field: 'foo',
            reason: 'bar'
        },
        {
            field: 'buzz',
            reason: ['bang', 'boom']
        },
        {
            field: 'bar',
            reason: 'foo'
        },
        {
            field: 'beep',
            reason: ['bleep', 'jeep']
        }
    ];
    it('matches with a string', () => {
        expect((0, invalidsContain_1.invalidsContain)(invalids, 'bar', (reason) => /^\w+$/.test(reason))).toBe(true);
    });
    it('matches with an array', () => {
        expect((0, invalidsContain_1.invalidsContain)(invalids, 'buzz', (reason) => reason === 'boom')).toBe(true);
    });
    it('detects non-match with a string', () => {
        expect((0, invalidsContain_1.invalidsContain)(invalids, 'foo', (reason) => /^\d+$/.test(reason))).toBe(false);
    });
    it('detects non-match with an array', () => {
        expect((0, invalidsContain_1.invalidsContain)(invalids, 'beep', (reason) => reason === 'horn')).toBe(false);
    });
});
//# sourceMappingURL=invalidsContain.test.js.map