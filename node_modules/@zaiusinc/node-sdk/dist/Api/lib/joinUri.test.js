"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
require("jest");
const joinUri_1 = require("./joinUri");
describe('joinUri', () => {
    it('combines uri parts', () => {
        expect((0, joinUri_1.joinUri)('http://foo', 'bar')).toEqual('http://foo/bar');
        expect((0, joinUri_1.joinUri)('http://foo', 'bar', 'baz')).toEqual('http://foo/bar/baz');
    });
    it("doesn't add a path separator when it's not necessary", () => {
        expect((0, joinUri_1.joinUri)('http://foo/', 'bar')).toEqual('http://foo/bar');
        expect((0, joinUri_1.joinUri)('http://foo', '/bar')).toEqual('http://foo/bar');
        expect((0, joinUri_1.joinUri)('http://foo', '/bar/', 'baz')).toEqual('http://foo/bar/baz');
    });
    it('removes a path separator when there is one on both sides of the join', () => {
        expect((0, joinUri_1.joinUri)('http://foo/', '/bar/')).toEqual('http://foo/bar/');
    });
});
//# sourceMappingURL=joinUri.test.js.map