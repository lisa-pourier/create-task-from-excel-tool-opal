import {Evaluation} from '../evaluations';
import {ElementType, NonFieldElement} from './Elements';

export interface Button extends NonFieldElement {
  type: ElementType.Button;
  label: string;
  action: string;
  help?: string;
  style?: 'primary' | 'none' | 'danger';
  disabled?: Evaluation;
}
