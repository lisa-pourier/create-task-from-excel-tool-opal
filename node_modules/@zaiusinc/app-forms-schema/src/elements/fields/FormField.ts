import {FormValue} from '../../data';
import {Evaluation, FieldValidation} from '../../evaluations';
import {FormElement} from '../Elements';

export interface FormField extends FormElement {
  key: string;
  label: string;
  help: string;
  defaultValue?: FormValue;
  required?: Evaluation;
  validations?: FieldValidation[];
}
